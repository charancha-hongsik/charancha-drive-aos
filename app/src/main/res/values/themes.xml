<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Charanchadriveaos" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/default_background</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- no action bar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.Charanchadrivesplash" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black_op_100</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@color/black_op_100</item>


        <!-- no action bar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

    <!-- Base application theme. -->
    <style name="Theme.Charanchadrivewhite100" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white_op_100</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/gray_50</item>

        <!-- no action bar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

    <style name="CustomBottomSheetDialog" parent="Theme.Design.Light.BottomSheetDialog">
        <!-- Customize BottomSheetDialog background -->
        <item name="bottomSheetStyle">@style/CustomBottomSheet</item>
    </style>

    <style name="CustomBottomSheet" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/radius24_topleftright_white</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.Charanchadrivegray50" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/gray_50</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/gray_50</item>

        <!-- no action bar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

    <style name="Theme.DetailManageScore" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white_op_95</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/gray_50</item>

        <!-- no action bar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

    <style name="Theme.MyInfoTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white_op_95</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/gray_50</item>

        <!-- no action bar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

    <style name="RegisterCarEditText" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorControlNormal">@color/gray_950</item> <!-- non focused line color -->
        <item name="colorControlActivated">@color/gray_950</item> <!-- focused line color -->
        <item name="android:editTextColor">@color/gray_950</item>  <!-- text color -->
        <item name="android:textColorHighlight">@color/gray_950</item> <!-- selected text color -->
        <item name="android:textColorHint">@color/gray_200</item>   <!-- hint color -->
    </style>



    <style name="EditCarText" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorControlNormal">@color/gray_950</item> <!-- non focused line color -->
        <item name="colorControlActivated">@color/gray_950</item> <!-- focused line color -->
        <item name="android:editTextColor">@color/gray_950</item>  <!-- text color -->
        <item name="android:textAppearance">@style/B1SB</item> <!-- textAppearance -->
        <item name="android:textColorHighlight">@color/gray_950</item> <!-- selected text color -->
        <item name="android:textColorHint">@color/gray_950</item>   <!-- hint color -->
        <item name="hintTextAppearance">@style/B1SB</item> <!-- hint textAppearance -->
    </style>

    <style name="Theme.MyPageTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/gray_950</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/gray_50</item>

        <!-- no action bar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

</resources>